export const metadata = {
  title: "Redux || Redux-Toolkit",
  description: "How to use Redux and Redux-Toolkit",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
